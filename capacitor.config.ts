import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ahmadfahri.istiqomahstock',
  appName: 'Istiqomah Stock',
  webDir: 'dist'
};

export default config;
